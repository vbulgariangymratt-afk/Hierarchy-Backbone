name: Build and Release Windows App

on:
  push:
    branches:
      - production

jobs:
  build-windows:
    permissions:
      contents: write
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm install

      - name: Build Tauri app for Windows
        run: npm run tauri:build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""

      - name: Generate Updater Metadata and Prepare Windows Asset
        shell: bash
        run: |
          find src-tauri/target/release/bundle/nsis -name "*.exe" -exec cp {} ./Backbone-Setup.exe \;
          node scripts/update-metadata.js
        env:
          PUBLIC_REPO_TOKEN: ${{ secrets.PUBLIC_REPO_TOKEN }}

      - name: Upload to Public Landing Page Releases
        uses: softprops/action-gh-release@v2
        with:
          repository: vbulgariangymratt-afk/Backbone-s-minimal-webpage
          token: ${{ secrets.PUBLIC_REPO_TOKEN }}
          tag_name: latest
          files: |
            Backbone-Setup.exe
          prerelease: false
          make_latest: true
